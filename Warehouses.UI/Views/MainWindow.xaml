<Window x:Class="Warehouses.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouses.UI.Views"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns:ff="clr-namespace:Warehouses.UI.MyControls"
        Title="LoginWindow" Height="642.722" Width="1092.89" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu IsMainMenu="True">
            <!-- New -->
            <MenuItem Header="_New">
                <!-- Input Receipt -->
                <MenuItem Header="Input Receipt" InputGestureText="Ctrl+I">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Inboxes" />
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Output Receipt -->
                <MenuItem Header="Output Receipt" InputGestureText="Ctrl+O">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Receipt" />
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Transaction Receipt -->
                <MenuItem Header="Transaction Receipt" InputGestureText="Ctrl+T">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ExitToApp" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <!-- Organization -->
                <MenuItem Header="Organization" InputGestureText="Ctrl+G">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="Globe" />
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Warehouse -->
                <MenuItem Header="Warehouse" InputGestureText="Ctrl+W">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="House" />
                    </MenuItem.Icon>
                </MenuItem>
                <!-- Branches -->
                <MenuItem Header="Branches" InputGestureText="Ctrl+B">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="SourceBranch" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            
            <!-- Edit -->
            <MenuItem Header="_Edit">
                <MenuItem Header="_Cut" Command="Cut">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentCut" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Copy" Command="Copy">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentCopy" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Paste" Command="Paste">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentPaste" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            
            <!-- Settings -->
            <MenuItem Header="_Settings" />
            
            <!-- Logout -->
            <MenuItem Header="_Logout" />
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ComboBox
                    materialDesign:HintAssist.Hint="Organization"
                    Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                    Margin="10 10 10 0">
                </ComboBox>
                <ComboBox
                    Grid.Row="1"
                    materialDesign:HintAssist.Hint="Branch"
                    Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                    Margin="10 10 10 0"/>
                <ComboBox
                    Grid.Row="2"                    
                    materialDesign:HintAssist.Hint="Warehouse"
                    Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                    Margin="10 10 10 0"/>
                <TreeView Grid.Row="3">
                    <TreeViewItem  />
                </TreeView>
            </Grid>
        </Grid>
    </Grid>
</Window>
